CREATE TABLE IF NOT EXISTS public.people (
    id INT,
    name VARCHAR(100),
    description TEXT
);

CREATE TABLE IF NOT EXISTS public.people_2_multi (
    id INT,
    name VARCHAR(100),
    description TEXT
);