CREATE TABLE IF NOT EXISTS public.jobs (
    id INT,
    name VARCHAR(100),
    description TEXT
);

CREATE TABLE IF NOT EXISTS public.jobs_2_multi (
    id INT,
    name VARCHAR(100),
    description TEXT
);